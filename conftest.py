# conftest.py — auto-generated for src/ layout by kanban dispatcher
import sys
from pathlib import Path

# Add src/ to path so tests can import the package
src_path = Path(__file__).parent / "src"
sys.path.insert(0, str(src_path))
